import LoadingPage from "@/app/(components)/loading-page"

// might not be instantly rendered
export default function Loading() { // must write export default, otherwise fail
    return (
        <LoadingPage/>
    )
}